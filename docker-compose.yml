---
version: '2'
services:
  login_api:
    build:
      context: ./api/login_api
      dockerfile: Dockerfile
    volumes:
      - ./api/login_api:/opt/login_api
    links:
      - db
    ports:
      - "4001:4001"
  db:
    image: postgres
    volumes:
      - ./data:/var/lib/postgresql
