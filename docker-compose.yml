---
version: '2'
services:
  login-api:
    build:
      context: ./api/login_api
      dockerfile: Dockerfile
    volumes:
      - ./api/login_api:/opt/login_api
    links:
      - db
  db:
    image: postgres
    volumes:
      - ./data:/var/lib/postgresql
